import React from 'react';
import { Typography, Paper, Divider } from '@material-ui/core';

export default () => (
  <React.Fragment>
    <Typography variant="display1">About</Typography>
    <Divider />
    <Typography variant="body2">Material-UI based React App</Typography>
  </React.Fragment>
);
